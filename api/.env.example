# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

DATABASE_URL="postgresql://app:!ChangeMe!@database:5432/app?schema=public"
# ─── Auth / JWT ───────────────────────────────────────────────
JWT_SECRET="change-me-in-production"

# ─── France Connect (integration env fcp-low.integ02) ─────────
# Credentials issued in the France Connect partner dashboard.
FC_CLIENT_ID=""
FC_CLIENT_SECRET=""
FC_BASE_URL="https://fcp-low.sbx.dev-franceconnect.fr/api/v2"
# Must be registered as a redirect URI in the France Connect dashboard.
FC_CALLBACK_URL="http://localhost/api/auth/france-connect/callback"
FC_SCOPE="openid email"
# Deep link the API redirects to after a successful France Connect login.
APP_REDIRECT_URL="application://auth"
